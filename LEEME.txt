Primer Paso (Opcional): instalar virtualenv
    1.pip install virtualenv
    2.virtualenv <nombre entorno>
    3.cd  <ruta del entorno>/bin/activate
Segundo Paso: En el directorio del repo descargado tipear pip install -r requirements.txt
 
COSAS DE NTLK QUE HAY QUE INSTALAR (NO LAS INSTALA PIP)
python -m textblob.download_corpora
